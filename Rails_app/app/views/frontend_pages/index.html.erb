<% @products.each do |product| %>
  <%= link_to image_tag(product.picture, :alt => product.name, :class => "product_image"), item_path(product), title: product.name %>
  <%#= product.name %>
<% end %>