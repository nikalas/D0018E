<%= form_for(sales_product) do |f| %>
  <% if sales_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sales_product.errors.count, "error") %> prohibited this sales_product from being saved:</h2>

      <ul>
      <% sales_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sale_id %>
    <%= f.text_field :sale_id %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
